export enum NotificationType {
  Liked = 'liked',
  Comment = 'comment',
  Post = 'post',
  App = 'app',
}

export enum NotificationTarget {
  Feed = 'feed',
  Story = 'story',
  Profile = 'profile',
}
